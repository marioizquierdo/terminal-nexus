{
  "name": "terminal-nexus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Terminal Nexus — the Grid tool, and the game's own menu (Milestone 3, Gate 3A)",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "check": "./scripts/check-repository.sh",
    "typecheck": "tsc --noEmit",
    "test": "./scripts/run-tests.sh node",
    "test:bun": "./scripts/run-tests.sh bun",
    "grid": "node bin/grid.ts",
    "terminal-nexus": "node bin/terminal-nexus.ts",
    "maps": "ls scenarios/*.map.json"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@opentui/core": "0.5.6"
  }
}
